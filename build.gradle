plugins {
    id("com.github.johnrengelman.shadow") version "7.1.2"
    id("io.micronaut.application") version "3.4.1"
}

version = "0.1"
group = "com.tim4it.payment.comparison"

repositories {
    mavenCentral()
}

ext {
    annotationApi           = "1.3.2"
    lombok                  = "1.18.24"
    jackson                 = "2.13.3"
    swaggerVersion          = '2.2.0'
    micronautOpenApiVersion = "4.2.0"
}

dependencies {
    implementation          project(":payment-comparison-dto")

    annotationProcessor     "io.micronaut:micronaut-http-validation"
    implementation          "io.micronaut:micronaut-http-client"
    implementation          "io.micronaut:micronaut-jackson-databind"

    implementation          "jakarta.annotation:jakarta.annotation-api"
    runtimeOnly             "ch.qos.logback:logback-classic"
    compileOnly             "org.graalvm.nativeimage:svm"

    implementation          "io.micronaut:micronaut-validation"
    implementation          "io.micronaut.reactor:micronaut-reactor"

    // lombok
    compileOnly             "org.projectlombok:lombok:${lombok}"
    annotationProcessor     "org.projectlombok:lombok:${lombok}"

    // jackson
    implementation          "com.fasterxml.jackson.core:jackson-core:${jackson}"
    implementation          "com.fasterxml.jackson.core:jackson-annotations:${jackson}"
    implementation          "com.fasterxml.jackson.datatype:jackson-datatype-jsr310"

    implementation          "javax.annotation:javax.annotation-api:1.3.2"

    // swagger, open api
    annotationProcessor     "io.micronaut:micronaut-management:3.5.1"
    annotationProcessor     "io.micronaut.openapi:micronaut-openapi"
    implementation          "io.swagger.core.v3:swagger-annotations:${swaggerVersion}"
    implementation          "io.micronaut.openapi:micronaut-openapi:${micronautOpenApiVersion}"

    // testing
   testCompileOnly          "io.micronaut:micronaut-http-client"
   testCompileOnly          "javax.annotation:javax.annotation-api:${annotationApi}"
   testCompileOnly          "org.projectlombok:lombok:${lombok}"
   testAnnotationProcessor  "org.projectlombok:lombok:${lombok}"
}

application {
    mainClass.set("com.tim4it.payment.comparison.PaymentComparisonApplication")
}

java {
    sourceCompatibility = JavaVersion.toVersion("11")
    targetCompatibility = JavaVersion.toVersion("11")
}

test {
    useJUnitPlatform()
}

distZip.enabled = distTar.enabled = shadowDistTar.enabled = shadowDistZip.enabled = false

graalvmNative.toolchainDetection = false
micronaut {
    runtime("netty")
    testRuntime("junit5")
    processing {
        incremental(true)
        annotations("example.micronaut.*")
    }
}



